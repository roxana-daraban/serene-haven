# Serene Haven — Frontend

Aplicația web React pentru Serene Haven. Documentația principală a repo-ului este în [**README-ul din rădăcină**](../README.md).

## Comenzi

```bash
npm install
npm run dev      # development
npm run build    # producție → dist/
npm run lint
npm run preview  # previzualizare build
```
